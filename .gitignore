##############################
# PYTHON / GENERAL
##############################

# Byte-compiled / cache
__pycache__/
*.py[cod]
*$py.class

# Serialized / temp artifacts
*.pkl
*.db
*.sqlite
*.log

# Virtual environments
.venv/
venv/
env/

# Packaging / build
build/
dist/
*.egg-info/

# Jupyter checkpoints
.ipynb_checkpoints/

# pytest / coverage / mypy
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/

# OS / editor files
.DS_Store
Thumbs.db
*.swp
*.swo
*.tmp
~$*
.vscode/
.idea/

##############################
# PROJECT DATA (TOP-LEVEL)
##############################

# Raw data (as sourced, unmodified) – keep this out of version control if too large
# data/raw/

# Intermediate + processed outputs generated by scripts/notebooks
data/interim/
data/processed/

# But keep documentation in data/
!data/README.md

##############################
# WEEKS 1–8 ARCHIVE
##############################

# Raw + interim + output data for Weeks 1–8 (generated or source-sensitive)
Weeks 1-8/data/raw/
Weeks 1-8/data/interim/
Weeks 1-8/data/output/

# Processed data/results for Weeks 1–8
Weeks 1-8/data/processed/
Weeks 1-8/results/runs/
Weeks 1-8/results/exports/

# Example: keep a specific reusable template/config if you want it versioned
# (uncomment if needed)
# !Weeks 1-8/data/processed/attack_prompts.json

# Registry file (auto-generated)
Weeks 1-8/data/registry.json

##############################
# REPORTS / GENERATED OUTPUT
##############################

# Generated reports (you can whitelist specific ones if you want them tracked)
reports/*
!reports/README.md

# Example: keep classifier report if you want it version-controlled
# !reports/context_classifier_report.txt

##############################
# DASHBOARD – NODE / NEXT.JS
##############################

# Node modules
src/dashboard/bias-frontend/node_modules/
src/dashboard/bias-frontend/.next/
src/dashboard/bias-frontend/.turbo/
src/dashboard/bias-frontend/.vercel/
src/dashboard/bias-frontend/coverage/

# Local env / config secrets
src/dashboard/bias-frontend/.env
src/dashboard/bias-frontend/.env.*
src/dashboard/bias-frontend/npm-debug.log*
src/dashboard/bias-frontend/yarn-error.log*
src/dashboard/bias-frontend/pnpm-debug.log*

# Don’t ignore lockfiles – they SHOULD be committed:
#   package-lock.json, pnpm-lock.yaml, etc.
# (so we intentionally do NOT list them here)

##############################
# MISC
##############################

# VS Code workspace at repo root (optional)
*.code-workspace

# Any temporary scratch folders you might create
scratch/
tmp/
